
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model NewsLetter {
  id Int @id @default(autoincrement())
  email String @unique
}